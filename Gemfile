source "https://rubygems.org"

# GitHub Pages build environment (includes Jekyll + jekyll-theme-cayman).
# Lets `bundle exec jekyll serve` build the same way Pages does.
gem "github-pages", group: :jekyll_plugins

# Lock to a Ruby-compatible webrick for local serving on Ruby 3+.
gem "webrick", "~> 1.8"
